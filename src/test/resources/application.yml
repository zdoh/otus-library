spring:
  datasource:
    url: jdbc:h2:mem:testdb;mode=mysql
    data: test_data.sql
    initialization-mode: always

  jpa:
    generate-ddl: false
    #generate-ddl: true

    hibernate:
      ddl-auto: none
      #ddl-auto: create-drop

    show-sql: true